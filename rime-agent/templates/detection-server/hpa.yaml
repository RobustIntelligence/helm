{{- if and .Values.rimeAgent.detectionServer.enabled .Values.rimeAgent.detectionServer.hpa.enabled }}
apiVersion: {{ include "rime-agent.hpa.apiVersion" . }}
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "rime-agent.fullname" . }}-{{ .Values.rimeAgent.detectionServer.name }}
  labels:
    app: {{ .Values.rimeAgent.detectionServer.name }}
    {{- include "rime-agent.labels" . | nindent 4 }}
    {{- with .Values.rimeAgent.detectionServer.hpa.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "rime-agent.fullname" . }}-{{ .Values.rimeAgent.detectionServer.name }}
  minReplicas: {{ .Values.rimeAgent.detectionServer.hpa.minReplicas }}
  maxReplicas: {{ .Values.rimeAgent.detectionServer.hpa.maxReplicas }}
  metrics:
{{ toYaml .Values.rimeAgent.detectionServer.hpa.metrics | indent 4 }}
{{- end }}
