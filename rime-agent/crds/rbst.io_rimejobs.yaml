---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.3
  creationTimestamp: null
  name: rimejobs.rbst.io
spec:
  group: rbst.io
  names:
    kind: RimeJob
    listKind: RimeJobList
    plural: rimejobs
    singular: rimejob
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.jobStatus
      name: JobStatus
      type: string
    - jsonPath: .spec.rimeSpec.testType
      name: TestType
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: "RimeJob is the Schema for the rimejob API. \n Enable object
          interface implementation generation for this type. Enable /status subresource
          for this type. Add a columns to \"kubectl get\" output for the CRD."
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: RimeJobSpec defines the desired state of RimeJob.
            properties:
              cancellationRequested:
                description: Flag to indicate that the RimeJob should be cancelled.
                type: boolean
              kubernetesSpec:
                description: Optional kubernetes resource settings. Values already
                  have defaults set by the underlying resource templates used as the
                  base for generating the resource spec.
                properties:
                  imageSpec:
                    description: ImageSpec defines the specification of the image
                      to run the job.
                    properties:
                      imageName:
                        type: string
                      imagePullSecrets:
                        type: string
                    type: object
                  resourceRequest:
                    description: ResourceRequests defines the resource request for
                      the RimeJob.
                    properties:
                      cpuRequestMillicores:
                        format: int64
                        type: integer
                      ramRequestMegabytes:
                        format: int64
                        type: integer
                    type: object
                type: object
              rimeSpec:
                description: RimeSpec defines the parameters needed for RIME engine.
                properties:
                  IntegrationVarFiles:
                    additionalProperties:
                      format: byte
                      type: string
                    description: Optional map of integration ID to integration variables
                      information bytes to be mounted as a file in the RIME container.
                    type: object
                  continuousTestSpec:
                    description: Optional settings for continuous test configuration.
                    properties:
                      firewallID:
                        type: string
                      overrideExistingBins:
                        type: boolean
                    type: object
                  enablePyspark:
                    description: Enable pyspark.
                    type: boolean
                  fileScanSpec:
                    description: Optional settings for file scan test configuration.
                    properties:
                      workspaceID:
                        type: string
                    type: object
                  rimeKeys:
                    description: RimeKeys defines the keys needed for RIME engine.
                    properties:
                      crossServiceKey:
                        description: CrossServiceKeyReference represents a reference
                          to the location of the cross service key.
                        properties:
                          keyPath:
                            type: string
                          secretName:
                            type: string
                        type: object
                      rimeJWT:
                        type: string
                    required:
                    - rimeJWT
                    type: object
                  stressTestSpec:
                    description: Optional settings for stress test configuration.
                    properties:
                      projectID:
                        type: string
                    type: object
                  testConfig:
                    format: byte
                    type: string
                  testType:
                    description: TestType is an enum for the type of test.
                    type: string
                required:
                - rimeKeys
                - testConfig
                - testType
                type: object
            required:
            - rimeSpec
            type: object
          status:
            description: RimeJobStatus defines the observed state of RimeJob.
            properties:
              jobStatus:
                description: JobStatus string that is a string mapping of the JobStatus
                  enum used by the StateDB.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
