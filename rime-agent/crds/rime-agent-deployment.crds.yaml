---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: riagents.rbst.io
spec:
  group: rbst.io
  names:
    kind: RIAgent
    listKind: RIAgentList
    plural: riagents
    singular: riagent
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.agentId
      name: AgentId
      type: string
    - jsonPath: .spec.version
      name: Version
      type: string
    - jsonPath: .status.agentStatus
      name: Status
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: RIAgent is the Schema for the RIAgent API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              RIAgentSpec defines the desired state of Agent.
              Currently we are only supporting the agent controller manager but
              this list will be expanded as we proceed in the project.
            properties:
              agentId:
                description: AgentID is the uuid of the agent assigned by the control
                  plane.
                type: string
              deploymentSpecs:
                description: |-
                  DeploymentSpecs is a list of serialized yaml files generated from the
                  control plane. The agent deployment controller will use this to deploy
                  the agent.
                items:
                  description: DeploymentSpec contains the serialized spec for a deployment.
                  properties:
                    installOrder:
                      description: |-
                        InstallOrder is an integer that specifies the order(step) in which this
                        set of yaml files should be installed. The agent deployment controller \
                        will use this to determine the order in which different elements of agent
                        are deployed.
                      type: integer
                    yamlSpecs:
                      description: YamlSpecs is a list of serialized yaml files for
                        the deployment.
                      items:
                        type: string
                      type: array
                  type: object
                type: array
              name:
                type: string
              version:
                description: Version of the agent
                type: string
            type: object
          status:
            description: RIAgentStatus defines the current state of the agent.
            properties:
              agentStatus:
                description: |-
                  AgentStatus is a string mapping of the Agent status
                  defined in AgentStatus field in agent proto
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
