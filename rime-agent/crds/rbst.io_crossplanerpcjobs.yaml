---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.3
  creationTimestamp: null
  name: crossplanerpcjobs.rbst.io
spec:
  group: rbst.io
  names:
    kind: CrossPlaneRPCJob
    listKind: CrossPlaneRPCJobList
    plural: crossplanerpcjobs
    singular: crossplanerpcjob
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.jobStatus
      name: JobStatus
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: "CrossPlaneRPCJob is the Schema for the crossplanerpcjobs API
          \n Enable object interface implementation generation for this type. Enable
          /status subresource for this type. Add a columns to \"kubectl get\" output
          for the CRD."
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: CrossPlaneRPCJobSpec defines the desired state of CrossPlaneRPCJob.
            properties:
              cancellationRequested:
                description: Flag to indicate that the CrossPlaneJob should be cancelled.
                type: boolean
              crossPlaneRPCSpec:
                description: CrossPlaneRPCSpec defines the parameters needed for a
                  cross plane rpc.
                properties:
                  crossPlaneTaskConfig:
                    description: CrossPlaneTaskConfig is the CrossPlaneRequest proto
                      for a CrossPlaneTask that has been marshalled.
                    format: byte
                    type: string
                required:
                - crossPlaneTaskConfig
                type: object
              crossServiceKeyRef:
                description: Reference to the cross service key. Only required when
                  running on an Internal agent.
                properties:
                  keyPath:
                    type: string
                  secretName:
                    type: string
                type: object
            required:
            - crossPlaneRPCSpec
            type: object
          status:
            description: CrossPlaneRPCJobStatus defines the observed state of a CrossPlaneRPCJob.
            properties:
              jobStatus:
                description: JobStatus string that is a string mapping of the JobStatus
                  enum used by the StateDB.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
