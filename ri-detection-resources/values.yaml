# Common annotations added to all K8s resources
commonAnnotations: {}
# Common labels added to all K8s resources
commonLabels: {}

# -- Image specification for the RI detection resources.
# @default -- (see individual values in `values.yaml`)
images:
  imagePullSecrets:
    - name: ri-regcred
  modelServerImage:
    registry: "docker.io"
    name: "robustintelligencehq/firewall-model-server:latest"
    pullPolicy: "Always"
  firewallBackendImage:
    registry: "docker.io"
    name: "robustintelligencehq/firewall-backend:latest"
    pullPolicy: "Always"

# -- Values for the internal RI K8 secret used by the detection resources.
# @default -- (see individual values in `values`.yaml)
secrets:
  # If existingIntegrationSecretsName is set, the secret will not be created.
  # Must have huggingfaceAPIKey key set.
  existingIntegrationSecretsName: ""

# -- `modelServers` K8s-level configurations
# @default -- (see individual values in `values.yaml`)
modelServers:
  # The following section of model server config values between the comments
  # is autogenerated by the autogen script. The start and end delimiter
  # comments should not be changed and are used to identify the section that
  # should be autogenerated.
  ## START MODEL SERVER VALUES
  promptInjectionBinary:
    enabled: true
    maxReplicas: 3
    minReplicas: 1
    # Settings for loading the model from an artifact cached in the Docker image
    # instead of downloading it during server startup time.
    useCachedArtifact:
      enabled: false
      dockerImageOverride:
        registry: "docker.io"
        name: "robustintelligencehq/firewall-model-server-prompt-injection-binary:latest"
        pullPolicy: "Always"
    # Use an existing model server outside the cluster instead of spinning up a
    # model server in this Helm deployment.
    # Only enable this for local Minikube.
    remoteModelServer:
      enabled: false
      address: ""
    resources:
      requests:
        memory: "1700Mi"
        cpu: "1000m"
        gpu: 0
      limits:
        memory: "1700Mi"
        cpu: "5000m"
        gpu: 0
  toxicityIntentMultiLabel:
    enabled: true
    maxReplicas: 3
    minReplicas: 1
    # Settings for loading the model from an artifact cached in the Docker image
    # instead of downloading it during server startup time.
    useCachedArtifact:
      enabled: false
      dockerImageOverride:
        registry: "docker.io"
        name: "robustintelligencehq/firewall-model-server-toxicity-intent-multi-label:latest"
        pullPolicy: "Always"
    # Use an existing model server outside the cluster instead of spinning up a
    # model server in this Helm deployment.
    # Only enable this for local Minikube.
    remoteModelServer:
      enabled: false
      address: ""
    resources:
      requests:
        memory: "5000Mi"
        cpu: "1000m"
        gpu: 0
      limits:
        memory: "5000Mi"
        cpu: "5000m"
        gpu: 0
  toxicity:
    enabled: true
    maxReplicas: 3
    minReplicas: 1
    # Settings for loading the model from an artifact cached in the Docker image
    # instead of downloading it during server startup time.
    useCachedArtifact:
      enabled: false
      dockerImageOverride:
        registry: "docker.io"
        name: "robustintelligencehq/firewall-model-server-toxicity:latest"
        pullPolicy: "Always"
    # Use an existing model server outside the cluster instead of spinning up a
    # model server in this Helm deployment.
    # Only enable this for local Minikube.
    remoteModelServer:
      enabled: false
      address: ""
    resources:
      requests:
        memory: "1800Mi"
        cpu: "1000m"
        gpu: 0
      limits:
        memory: "1800Mi"
        cpu: "5000m"
        gpu: 0
  refusalHarmfulDetector:
    enabled: true
    maxReplicas: 3
    minReplicas: 1
    # Settings for loading the model from an artifact cached in the Docker image
    # instead of downloading it during server startup time.
    useCachedArtifact:
      enabled: false
      dockerImageOverride:
        registry: "docker.io"
        name: "robustintelligencehq/firewall-model-server-refusal-harmful-detector:latest"
        pullPolicy: "Always"
    # Use an existing model server outside the cluster instead of spinning up a
    # model server in this Helm deployment.
    # Only enable this for local Minikube.
    remoteModelServer:
      enabled: false
      address: ""
    resources:
      requests:
        memory: "1800Mi"
        cpu: "1000m"
        gpu: 0
      limits:
        memory: "1800Mi"
        cpu: "5000m"
        gpu: 0
  factualInconsistency:
    enabled: true
    maxReplicas: 3
    minReplicas: 1
    # Settings for loading the model from an artifact cached in the Docker image
    # instead of downloading it during server startup time.
    useCachedArtifact:
      enabled: false
      dockerImageOverride:
        registry: "docker.io"
        name: "robustintelligencehq/firewall-model-server-factual-inconsistency:latest"
        pullPolicy: "Always"
    # Use an existing model server outside the cluster instead of spinning up a
    # model server in this Helm deployment.
    # Only enable this for local Minikube.
    remoteModelServer:
      enabled: false
      address: ""
    resources:
      requests:
        memory: "2600Mi"
        cpu: "1000m"
        gpu: 0
      limits:
        memory: "2600Mi"
        cpu: "5000m"
        gpu: 0
  languageDetection:
    enabled: true
    maxReplicas: 3
    minReplicas: 1
    # Settings for loading the model from an artifact cached in the Docker image
    # instead of downloading it during server startup time.
    useCachedArtifact:
      enabled: false
      dockerImageOverride:
        registry: "docker.io"
        name: "robustintelligencehq/firewall-model-server-language-detection:latest"
        pullPolicy: "Always"
    # Use an existing model server outside the cluster instead of spinning up a
    # model server in this Helm deployment.
    # Only enable this for local Minikube.
    remoteModelServer:
      enabled: false
      address: ""
    resources:
      requests:
        memory: "1000Mi"
        cpu: "1000m"
        gpu: 0
      limits:
        memory: "1000Mi"
        cpu: "5000m"
        gpu: 0
  textEmbedding:
    enabled: true
    maxReplicas: 3
    minReplicas: 1
    # Settings for loading the model from an artifact cached in the Docker image
    # instead of downloading it during server startup time.
    useCachedArtifact:
      enabled: false
      dockerImageOverride:
        registry: "docker.io"
        name: "robustintelligencehq/firewall-model-server-text-embedding:latest"
        pullPolicy: "Always"
    # Use an existing model server outside the cluster instead of spinning up a
    # model server in this Helm deployment.
    # Only enable this for local Minikube.
    remoteModelServer:
      enabled: false
      address: ""
    resources:
      requests:
        memory: "1300Mi"
        cpu: "1000m"
        gpu: 0
      limits:
        memory: "1300Mi"
        cpu: "5000m"
        gpu: 0
  toxicityInterpretability:
    enabled: true
    maxReplicas: 3
    minReplicas: 1
    # Settings for loading the model from an artifact cached in the Docker image
    # instead of downloading it during server startup time.
    useCachedArtifact:
      enabled: false
      dockerImageOverride:
        registry: "docker.io"
        name: "robustintelligencehq/firewall-model-server-toxicity-interpretability:latest"
        pullPolicy: "Always"
    # Use an existing model server outside the cluster instead of spinning up a
    # model server in this Helm deployment.
    # Only enable this for local Minikube.
    remoteModelServer:
      enabled: false
      address: ""
    resources:
      requests:
        memory: "1700Mi"
        cpu: "1000m"
        gpu: 0
      limits:
        memory: "1700Mi"
        cpu: "5000m"
        gpu: 0
  factualInconsistencyJa:
    enabled: true
    maxReplicas: 3
    minReplicas: 1
    # Settings for loading the model from an artifact cached in the Docker image
    # instead of downloading it during server startup time.
    useCachedArtifact:
      enabled: false
      dockerImageOverride:
        registry: "docker.io"
        name: "robustintelligencehq/firewall-model-server-factual-inconsistency-ja:latest"
        pullPolicy: "Always"
    # Use an existing model server outside the cluster instead of spinning up a
    # model server in this Helm deployment.
    # Only enable this for local Minikube.
    remoteModelServer:
      enabled: false
      address: ""
    resources:
      requests:
        memory: "3300Mi"
        cpu: "1000m"
        gpu: 0
      limits:
        memory: "3300Mi"
        cpu: "5000m"
        gpu: 0
  textEmbeddingJa:
    enabled: true
    maxReplicas: 3
    minReplicas: 1
    # Settings for loading the model from an artifact cached in the Docker image
    # instead of downloading it during server startup time.
    useCachedArtifact:
      enabled: false
      dockerImageOverride:
        registry: "docker.io"
        name: "robustintelligencehq/firewall-model-server-text-embedding-ja:latest"
        pullPolicy: "Always"
    # Use an existing model server outside the cluster instead of spinning up a
    # model server in this Helm deployment.
    # Only enable this for local Minikube.
    remoteModelServer:
      enabled: false
      address: ""
    resources:
      requests:
        memory: "1400Mi"
        cpu: "1000m"
        gpu: 0
      limits:
        memory: "1400Mi"
        cpu: "5000m"
        gpu: 0
  toxicityJa:
    enabled: true
    maxReplicas: 3
    minReplicas: 1
    # Settings for loading the model from an artifact cached in the Docker image
    # instead of downloading it during server startup time.
    useCachedArtifact:
      enabled: false
      dockerImageOverride:
        registry: "docker.io"
        name: "robustintelligencehq/firewall-model-server-toxicity-ja:latest"
        pullPolicy: "Always"
    # Use an existing model server outside the cluster instead of spinning up a
    # model server in this Helm deployment.
    # Only enable this for local Minikube.
    remoteModelServer:
      enabled: false
      address: ""
    resources:
      requests:
        memory: "1800Mi"
        cpu: "1000m"
        gpu: 0
      limits:
        memory: "1800Mi"
        cpu: "5000m"
        gpu: 0
  xlmMultilingualToxicity:
    enabled: true
    maxReplicas: 3
    minReplicas: 1
    # Settings for loading the model from an artifact cached in the Docker image
    # instead of downloading it during server startup time.
    useCachedArtifact:
      enabled: false
      dockerImageOverride:
        registry: "docker.io"
        name: "robustintelligencehq/firewall-model-server-xlm-multilingual-toxicity:latest"
        pullPolicy: "Always"
    # Use an existing model server outside the cluster instead of spinning up a
    # model server in this Helm deployment.
    # Only enable this for local Minikube.
    remoteModelServer:
      enabled: false
      address: ""
    resources:
      requests:
        memory: "3500Mi"
        cpu: "1000m"
        gpu: 0
      limits:
        memory: "3500Mi"
        cpu: "5000m"
        gpu: 0
  xlmMultilingualHarmful:
    enabled: true
    maxReplicas: 3
    minReplicas: 1
    # Settings for loading the model from an artifact cached in the Docker image
    # instead of downloading it during server startup time.
    useCachedArtifact:
      enabled: false
      dockerImageOverride:
        registry: "docker.io"
        name: "robustintelligencehq/firewall-model-server-xlm-multilingual-harmful:latest"
        pullPolicy: "Always"
    # Use an existing model server outside the cluster instead of spinning up a
    # model server in this Helm deployment.
    # Only enable this for local Minikube.
    remoteModelServer:
      enabled: false
      address: ""
    resources:
      requests:
        memory: "3500Mi"
        cpu: "1000m"
        gpu: 0
      limits:
        memory: "3500Mi"
        cpu: "5000m"
        gpu: 0
    ## END MODEL SERVER VALUES
