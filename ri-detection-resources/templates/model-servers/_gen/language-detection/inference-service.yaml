{{- if and .Values.modelServers.languageDetection.enabled (not .Values.modelServers.languageDetection.remoteModelServer.enabled) }}
# Autogenerated by a script. DO NOT EDIT.
apiVersion: serving.kserve.io/v1beta1
kind: InferenceService
metadata:
  name: language-detection
  annotations:
    {{- include "ri-detection-resources.annotations" . | nindent 4 }}
spec:
  predictor:
    {{- with .Values.images.imagePullSecrets }}
    imagePullSecrets:
      {{- toYaml . | nindent 8 }}
    {{- end }}
    minReplicas: {{ .Values.modelServers.languageDetection.minReplicas }}
    maxReplicas: {{ .Values.modelServers.languageDetection.maxReplicas }}
    containers:
      - name: language-detection
      {{- if .Values.modelServers.languageDetection.useCachedArtifact.enabled }}
        image: "{{ .Values.modelServers.languageDetection.useCachedArtifact.dockerImageOverride.registry }}/{{ .Values.modelServers.languageDetection.useCachedArtifact.dockerImageOverride.name }}"
        imagePullPolicy: {{ .Values.modelServers.languageDetection.useCachedArtifact.dockerImageOverride.pullPolicy }}
      {{- else }}
        image: "{{ .Values.images.modelServerImage.registry}}/{{ .Values.images.modelServerImage.name }}"
        imagePullPolicy: {{ .Values.images.modelServerImage.pullPolicy }}
      {{- end }}
        env:
        {{- if not .Values.modelServers.languageDetection.useCachedArtifact.enabled }}
          - name: HUGGINGFACE_API_KEY
            valueFrom:
              secretKeyRef:
                name: {{ .Values.secrets.existingIntegrationSecretsName }}
                key: huggingfaceAPIKey
        {{- end }}
        ports:
          - name: rest
            protocol: TCP
            containerPort: 8080
        command:
          - "run_server"
        args:
          - "--model-config-path=/model_server/model-settings.json"
          - "--cache-dir=/model_cache"
        startupProbe:
          httpGet:
            path: /v2/models/language-detection/ready
            port: 8080
          failureThreshold: 20
          periodSeconds: 5
        readinessProbe:
          httpGet:
            path: /v2/models/language-detection/ready
            port: 8080
          periodSeconds: 5
        resources:
          requests:
            memory: {{ .Values.modelServers.languageDetection.resources.requests.memory }}
            cpu: {{ .Values.modelServers.languageDetection.resources.requests.cpu }}
          limits:
            memory: {{ .Values.modelServers.languageDetection.resources.limits.memory }}
            cpu: {{ .Values.modelServers.languageDetection.resources.limits.cpu }}
        volumeMounts:
          - name: language-detection-config
            mountPath: "/model_server"
            readOnly: true
          - name: language-detection-model-cache
            mountPath: "/model_cache"
            readOnly: false
    volumes:
      # Volumes are defined at the Pod level, then mounted into containers within that Pod
      - name: language-detection-config
        configMap:
          name: {{ include "ri-detection-resources.fullname" . }}-language-detection-conf
          items:
            - key: "model-settings.json"
              path: "model-settings.json"
      - name: language-detection-model-cache
        emptyDir: { }
{{- end }}
