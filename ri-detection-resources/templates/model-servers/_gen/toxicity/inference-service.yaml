{{- if and .Values.modelServers.toxicity.enabled (not .Values.modelServers.toxicity.remoteModelServer.enabled) }}
# Autogenerated by a script. DO NOT EDIT.
apiVersion: serving.kserve.io/v1beta1
kind: InferenceService
metadata:
  name: toxicity
  annotations:
    {{- include "ri-detection-resources.annotations" . | nindent 4 }}
spec:
  predictor:
    {{- with .Values.images.imagePullSecrets }}
    imagePullSecrets:
      {{- toYaml . | nindent 8 }}
    {{- end }}
    minReplicas: {{ .Values.modelServers.toxicity.minReplicas }}
    maxReplicas: {{ .Values.modelServers.toxicity.maxReplicas }}
    securityContext:
      runAsUser: 1000
      runAsGroup: 1000
      fsGroup: 1000
    containers:
      - name: toxicity
      {{- if .Values.modelServers.toxicity.useCachedArtifact.enabled }}
        image: "{{ .Values.modelServers.toxicity.useCachedArtifact.dockerImageOverride.registry }}/{{ .Values.modelServers.toxicity.useCachedArtifact.dockerImageOverride.name }}"
        imagePullPolicy: {{ .Values.modelServers.toxicity.useCachedArtifact.dockerImageOverride.pullPolicy }}
      {{- else }}
        image: "{{ .Values.images.modelServerImage.registry}}/{{ .Values.images.modelServerImage.name }}"
        imagePullPolicy: {{ .Values.images.modelServerImage.pullPolicy }}
      {{- end }}
        env:
        {{- if not .Values.modelServers.toxicity.useCachedArtifact.enabled }}
          - name: HUGGINGFACE_API_KEY
            valueFrom:
              secretKeyRef:
                name: {{ .Values.secrets.existingIntegrationSecretsName }}
                key: huggingfaceAPIKey
        {{- end }}
        ports:
          - name: rest
            protocol: TCP
            containerPort: 8080
        command:
          - "run_server"
        args:
          - "--model-config-path=/model_server/model-settings.json"
          - "--cache-dir=/model_cache"
        startupProbe:
          httpGet:
            path: /v2/models/toxicity/ready
            port: 8080
          failureThreshold: 20
          periodSeconds: 5
        readinessProbe:
          httpGet:
            path: /v2/models/toxicity/ready
            port: 8080
          periodSeconds: 5
        resources:
          requests:
            memory: {{ .Values.modelServers.toxicity.resources.requests.memory }}
            cpu: {{ .Values.modelServers.toxicity.resources.requests.cpu }}
          limits:
            memory: {{ .Values.modelServers.toxicity.resources.limits.memory }}
            cpu: {{ .Values.modelServers.toxicity.resources.limits.cpu }}
        volumeMounts:
          - name: toxicity-config
            mountPath: "/model_server"
            readOnly: true
          - name: toxicity-model-cache
            mountPath: "/model_cache"
            readOnly: false
    volumes:
      # Volumes are defined at the Pod level, then mounted into containers within that Pod
      - name: toxicity-config
        configMap:
          name: {{ include "ri-detection-resources.fullname" . }}-toxicity-conf
          items:
            - key: "model-settings.json"
              path: "model-settings.json"
      - name: toxicity-model-cache
        emptyDir: { }
{{- end }}
