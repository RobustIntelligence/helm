{{- if and .Values.modelServers.factualInconsistencyJa.enabled (not .Values.modelServers.factualInconsistencyJa.remoteModelServer.enabled) }}
# Autogenerated by a script. DO NOT EDIT.
apiVersion: serving.kserve.io/v1beta1
kind: InferenceService
metadata:
  name: factual-inconsistency-ja
  annotations:
    {{- include "ri-detection-resources.annotations" . | nindent 4 }}
spec:
  predictor:
    {{- with .Values.images.imagePullSecrets }}
    imagePullSecrets:
      {{- toYaml . | nindent 8 }}
    {{- end }}
    minReplicas: {{ .Values.modelServers.factualInconsistencyJa.minReplicas }}
    maxReplicas: {{ .Values.modelServers.factualInconsistencyJa.maxReplicas }}
    securityContext:
      runAsUser: 1000
      runAsGroup: 1000
      fsGroup: 1000
    containers:
      - name: factual-inconsistency-ja
      {{- if .Values.modelServers.factualInconsistencyJa.useCachedArtifact.enabled }}
        image: "{{ .Values.modelServers.factualInconsistencyJa.useCachedArtifact.dockerImageOverride.registry }}/{{ .Values.modelServers.factualInconsistencyJa.useCachedArtifact.dockerImageOverride.name }}"
        imagePullPolicy: {{ .Values.modelServers.factualInconsistencyJa.useCachedArtifact.dockerImageOverride.pullPolicy }}
      {{- else }}
        image: "{{ .Values.images.modelServerImage.registry}}/{{ .Values.images.modelServerImage.name }}"
        imagePullPolicy: {{ .Values.images.modelServerImage.pullPolicy }}
      {{- end }}
        env:
        {{- if not .Values.modelServers.factualInconsistencyJa.useCachedArtifact.enabled }}
          - name: HUGGINGFACE_API_KEY
            valueFrom:
              secretKeyRef:
                name: {{ .Values.secrets.existingIntegrationSecretsName }}
                key: huggingfaceAPIKey
        {{- end }}
        ports:
          - name: rest
            protocol: TCP
            containerPort: 8080
        command:
          - "run_server"
        args:
          - "--model-config-path=/model_server/model-settings.json"
          - "--cache-dir=/model_cache"
        startupProbe:
          httpGet:
            path: /v2/models/factual-inconsistency-ja/ready
            port: 8080
          failureThreshold: 20
          periodSeconds: 5
        readinessProbe:
          httpGet:
            path: /v2/models/factual-inconsistency-ja/ready
            port: 8080
          periodSeconds: 5
        resources:
          requests:
            memory: {{ .Values.modelServers.factualInconsistencyJa.resources.requests.memory }}
            cpu: {{ .Values.modelServers.factualInconsistencyJa.resources.requests.cpu }}
          limits:
            memory: {{ .Values.modelServers.factualInconsistencyJa.resources.limits.memory }}
            cpu: {{ .Values.modelServers.factualInconsistencyJa.resources.limits.cpu }}
        volumeMounts:
          - name: factual-inconsistency-ja-config
            mountPath: "/model_server"
            readOnly: true
          - name: factual-inconsistency-ja-model-cache
            mountPath: "/model_cache"
            readOnly: false
    volumes:
      # Volumes are defined at the Pod level, then mounted into containers within that Pod
      - name: factual-inconsistency-ja-config
        configMap:
          name: {{ include "ri-detection-resources.fullname" . }}-factual-inconsistency-ja-conf
          items:
            - key: "model-settings.json"
              path: "model-settings.json"
      - name: factual-inconsistency-ja-model-cache
        emptyDir: { }
{{- end }}
