{{- if and .Values.modelServers.refusalHarmfulDetector.enabled (not .Values.modelServers.refusalHarmfulDetector.remoteModelServer.enabled) }}
# Autogenerated by a script. DO NOT EDIT.
apiVersion: serving.kserve.io/v1beta1
kind: InferenceService
metadata:
  name: refusal-harmful-detector
  annotations:
    {{- include "ri-detection-resources.annotations" . | nindent 4 }}
spec:
  predictor:
    {{- with .Values.images.imagePullSecrets }}
    imagePullSecrets:
      {{- toYaml . | nindent 8 }}
    {{- end }}
    minReplicas: {{ .Values.modelServers.refusalHarmfulDetector.minReplicas }}
    maxReplicas: {{ .Values.modelServers.refusalHarmfulDetector.maxReplicas }}
    securityContext:
      runAsUser: 1000
      runAsGroup: 1000
      fsGroup: 1000
    containers:
      - name: refusal-harmful-detector
      {{- if .Values.modelServers.refusalHarmfulDetector.useCachedArtifact.enabled }}
        image: "{{ .Values.modelServers.refusalHarmfulDetector.useCachedArtifact.dockerImageOverride.registry }}/{{ .Values.modelServers.refusalHarmfulDetector.useCachedArtifact.dockerImageOverride.name }}"
        imagePullPolicy: {{ .Values.modelServers.refusalHarmfulDetector.useCachedArtifact.dockerImageOverride.pullPolicy }}
      {{- else }}
        image: "{{ .Values.images.modelServerImage.registry}}/{{ .Values.images.modelServerImage.name }}"
        imagePullPolicy: {{ .Values.images.modelServerImage.pullPolicy }}
      {{- end }}
        env:
        {{- if not .Values.modelServers.refusalHarmfulDetector.useCachedArtifact.enabled }}
          - name: HUGGINGFACE_API_KEY
            valueFrom:
              secretKeyRef:
                name: {{ .Values.secrets.existingIntegrationSecretsName }}
                key: huggingfaceAPIKey
        {{- end }}
        ports:
          - name: rest
            protocol: TCP
            containerPort: 8080
        command:
          - "run_server"
        args:
          - "--model-config-path=/model_server/model-settings.json"
          - "--cache-dir=/model_cache"
        startupProbe:
          httpGet:
            path: /v2/models/refusal-harmful-detector/ready
            port: 8080
          failureThreshold: 20
          periodSeconds: 5
        readinessProbe:
          httpGet:
            path: /v2/models/refusal-harmful-detector/ready
            port: 8080
          periodSeconds: 5
        resources:
          requests:
            memory: {{ .Values.modelServers.refusalHarmfulDetector.resources.requests.memory }}
            cpu: {{ .Values.modelServers.refusalHarmfulDetector.resources.requests.cpu }}
          limits:
            memory: {{ .Values.modelServers.refusalHarmfulDetector.resources.limits.memory }}
            cpu: {{ .Values.modelServers.refusalHarmfulDetector.resources.limits.cpu }}
        volumeMounts:
          - name: refusal-harmful-detector-config
            mountPath: "/model_server"
            readOnly: true
          - name: refusal-harmful-detector-model-cache
            mountPath: "/model_cache"
            readOnly: false
    volumes:
      # Volumes are defined at the Pod level, then mounted into containers within that Pod
      - name: refusal-harmful-detector-config
        configMap:
          name: {{ include "ri-detection-resources.fullname" . }}-refusal-harmful-detector-conf
          items:
            - key: "model-settings.json"
              path: "model-settings.json"
      - name: refusal-harmful-detector-model-cache
        emptyDir: { }
{{- end }}
