{{- if and .Values.modelServers.xlmMultilingualToxicity.enabled (not .Values.modelServers.xlmMultilingualToxicity.remoteModelServer.enabled) }}
# Autogenerated by a script. DO NOT EDIT.
apiVersion: serving.kserve.io/v1beta1
kind: InferenceService
metadata:
  name: xlm-multilingual-toxicity
  annotations:
    {{- include "ri-detection-resources.annotations" . | nindent 4 }}
spec:
  predictor:
    {{- with .Values.images.imagePullSecrets }}
    imagePullSecrets:
      {{- toYaml . | nindent 8 }}
    {{- end }}
    minReplicas: {{ .Values.modelServers.xlmMultilingualToxicity.minReplicas }}
    maxReplicas: {{ .Values.modelServers.xlmMultilingualToxicity.maxReplicas }}
    labels:
      app: xlmMultilingualToxicity
    securityContext:
      runAsUser: 1000
      runAsGroup: 1000
      fsGroup: 1000
    containers:
      - name: xlm-multilingual-toxicity
      {{- if .Values.modelServers.xlmMultilingualToxicity.useCachedArtifact.enabled }}
        image: "{{ .Values.modelServers.xlmMultilingualToxicity.useCachedArtifact.dockerImageOverride.registry }}/{{ .Values.modelServers.xlmMultilingualToxicity.useCachedArtifact.dockerImageOverride.name }}"
        imagePullPolicy: {{ .Values.modelServers.xlmMultilingualToxicity.useCachedArtifact.dockerImageOverride.pullPolicy }}
      {{- else }}
        image: "{{ .Values.images.modelServerImage.registry}}/{{ .Values.images.modelServerImage.name }}"
        imagePullPolicy: {{ .Values.images.modelServerImage.pullPolicy }}
      {{- end }}
        env:
        {{- if not .Values.modelServers.xlmMultilingualToxicity.useCachedArtifact.enabled }}
          - name: HUGGINGFACE_API_KEY
            valueFrom:
              secretKeyRef:
                name: {{ .Values.secrets.existingIntegrationSecretsName }}
                key: huggingfaceAPIKey
        {{- end }}
        ports:
          - name: rest
            protocol: TCP
            containerPort: 8080
        command:
          - "run_server"
        args:
          - "--model-config-path=/model_server/model-settings.json"
          - "--cache-dir=/model_cache"
        startupProbe:
          httpGet:
            path: /v2/models/xlm-multilingual-toxicity/ready
            port: 8080
          failureThreshold: 20
          periodSeconds: 5
        readinessProbe:
          httpGet:
            path: /v2/models/xlm-multilingual-toxicity/ready
            port: 8080
          periodSeconds: 5
        resources:
          requests:
            memory: {{ .Values.modelServers.xlmMultilingualToxicity.resources.requests.memory }}
            cpu: {{ .Values.modelServers.xlmMultilingualToxicity.resources.requests.cpu }}
            {{- if gt (int .Values.modelServers.xlmMultilingualToxicity.resources.requests.gpu) 0 }}
            nvidia.com/gpu: {{ .Values.modelServers.xlmMultilingualToxicity.resources.requests.gpu }}
            {{- end }}
          limits:
            memory: {{ .Values.modelServers.xlmMultilingualToxicity.resources.limits.memory }}
            cpu: {{ .Values.modelServers.xlmMultilingualToxicity.resources.limits.cpu }}
            {{- if gt (int .Values.modelServers.xlmMultilingualToxicity.resources.limits.gpu) 0 }}
            nvidia.com/gpu: {{ .Values.modelServers.xlmMultilingualToxicity.resources.limits.gpu }}
            {{- end }}
        volumeMounts:
          - name: xlm-multilingual-toxicity-config
            mountPath: "/model_server"
            readOnly: true
          - name: xlm-multilingual-toxicity-model-cache
            mountPath: "/model_cache"
            readOnly: false
    {{- if gt (int .Values.modelServers.xlmMultilingualToxicity.resources.limits.gpu) 0 }}
    tolerations:
    - effect: NoSchedule
      key: dedicated
      operator: Equal
      value: gpu-dedicated
    {{- else }}
    {{- with .Values.modelServers.deployment.tolerations }}
    tolerations:
      {{- toYaml . | nindent 12 }}
    {{- end }}
    {{- end }}

    topologySpreadConstraints:
    - maxSkew: 1
      topologyKey: "topology.kubernetes.io/zone"
      whenUnsatisfiable: ScheduleAnyway
      labelSelector:
        matchLabels:
          app: xlmMultilingualToxicity

    affinity:
      podAntiAffinity:
        preferredDuringSchedulingIgnoredDuringExecution:
        - podAffinityTerm:
            labelSelector:
              matchLabels:
                app: xlmMultilingualToxicity
            topologyKey: topology.kubernetes.io/zone
          weight: 100
        - podAffinityTerm:
            labelSelector:
              matchLabels:
                app: xlmMultilingualToxicity
            topologyKey: kubernetes.io/hostname
          weight: 99
    volumes:
      # Volumes are defined at the Pod level, then mounted into containers within that Pod
      - name: xlm-multilingual-toxicity-config
        configMap:
          name: {{ include "ri-detection-resources.fullname" . }}-xlm-multilingual-toxicity-conf
          items:
            - key: "model-settings.json"
              path: "model-settings.json"
      - name: xlm-multilingual-toxicity-model-cache
        emptyDir: { }
{{- end }}
