{{- if and .Values.modelServers.promptInjectionMulticlass.enabled (not .Values.modelServers.promptInjectionMulticlass.remoteModelServer.enabled) }}
# Autogenerated by a script. DO NOT EDIT.
apiVersion: serving.kserve.io/v1beta1
kind: InferenceService
metadata:
  name: prompt-injection-multiclass
  annotations:
    {{- include "ri-detection-resources.annotations" . | nindent 4 }}
spec:
  predictor:
    {{- with .Values.images.imagePullSecrets }}
    imagePullSecrets:
      {{- toYaml . | nindent 8 }}
    {{- end }}
    minReplicas: {{ .Values.modelServers.promptInjectionMulticlass.minReplicas }}
    maxReplicas: {{ .Values.modelServers.promptInjectionMulticlass.maxReplicas }}
    securityContext:
      runAsUser: 1000
      runAsGroup: 1000
      fsGroup: 1000
    containers:
      - name: prompt-injection-multiclass
      {{- if .Values.modelServers.promptInjectionMulticlass.useCachedArtifact.enabled }}
        image: "{{ .Values.modelServers.promptInjectionMulticlass.useCachedArtifact.dockerImageOverride.registry }}/{{ .Values.modelServers.promptInjectionMulticlass.useCachedArtifact.dockerImageOverride.name }}"
        imagePullPolicy: {{ .Values.modelServers.promptInjectionMulticlass.useCachedArtifact.dockerImageOverride.pullPolicy }}
      {{- else }}
        image: "{{ .Values.images.modelServerImage.registry}}/{{ .Values.images.modelServerImage.name }}"
        imagePullPolicy: {{ .Values.images.modelServerImage.pullPolicy }}
      {{- end }}
        env:
        {{- if not .Values.modelServers.promptInjectionMulticlass.useCachedArtifact.enabled }}
          - name: HUGGINGFACE_API_KEY
            valueFrom:
              secretKeyRef:
                name: {{ .Values.secrets.existingIntegrationSecretsName }}
                key: huggingfaceAPIKey
        {{- end }}
        ports:
          - name: rest
            protocol: TCP
            containerPort: 8080
        command:
          - "run_server"
        args:
          - "--model-config-path=/model_server/model-settings.json"
          - "--cache-dir=/model_cache"
        startupProbe:
          httpGet:
            path: /v2/models/prompt-injection-multiclass/ready
            port: 8080
          failureThreshold: 20
          periodSeconds: 5
        readinessProbe:
          httpGet:
            path: /v2/models/prompt-injection-multiclass/ready
            port: 8080
          periodSeconds: 5
        resources:
          requests:
            memory: {{ .Values.modelServers.promptInjectionMulticlass.resources.requests.memory }}
            cpu: {{ .Values.modelServers.promptInjectionMulticlass.resources.requests.cpu }}
            {{- if gt (int .Values.modelServers.promptInjectionMulticlass.resources.requests.gpu) 0 }}
            nvidia.com/gpu: {{ .Values.modelServers.promptInjectionMulticlass.resources.requests.gpu }}
            {{- end }}
          limits:
            memory: {{ .Values.modelServers.promptInjectionMulticlass.resources.limits.memory }}
            cpu: {{ .Values.modelServers.promptInjectionMulticlass.resources.limits.cpu }}
            {{- if gt (int .Values.modelServers.promptInjectionMulticlass.resources.limits.gpu) 0 }}
            nvidia.com/gpu: {{ .Values.modelServers.promptInjectionMulticlass.resources.limits.gpu }}
            {{- end }}
        volumeMounts:
          - name: prompt-injection-multiclass-config
            mountPath: "/model_server"
            readOnly: true
          - name: prompt-injection-multiclass-model-cache
            mountPath: "/model_cache"
            readOnly: false
    {{- if gt (int .Values.modelServers.promptInjectionMulticlass.resources.limits.gpu) 0 }}
    tolerations:
    - effect: NoSchedule
      key: dedicated
      operator: Equal
      value: gpu-dedicated
    {{- end }}
    volumes:
      # Volumes are defined at the Pod level, then mounted into containers within that Pod
      - name: prompt-injection-multiclass-config
        configMap:
          name: {{ include "ri-detection-resources.fullname" . }}-prompt-injection-multiclass-conf
          items:
            - key: "model-settings.json"
              path: "model-settings.json"
      - name: prompt-injection-multiclass-model-cache
        emptyDir: { }
{{- end }}
