{{- if and .Values.modelServers.toxicityIntentMultiLabel.enabled (not .Values.modelServers.toxicityIntentMultiLabel.remoteModelServer.enabled) }}
# Autogenerated by a script. DO NOT EDIT.
apiVersion: serving.kserve.io/v1beta1
kind: InferenceService
metadata:
  name: toxicity-intent-multi-label
  annotations:
    {{- include "ri-detection-resources.annotations" . | nindent 4 }}
spec:
  predictor:
    {{- with .Values.images.imagePullSecrets }}
    imagePullSecrets:
      {{- toYaml . | nindent 8 }}
    {{- end }}
    minReplicas: {{ .Values.modelServers.toxicityIntentMultiLabel.minReplicas }}
    maxReplicas: {{ .Values.modelServers.toxicityIntentMultiLabel.maxReplicas }}
    labels:
      app: toxicityIntentMultiLabel
    securityContext:
      runAsUser: 1000
      runAsGroup: 1000
      fsGroup: 1000
    containers:
      - name: toxicity-intent-multi-label
      {{- if .Values.modelServers.toxicityIntentMultiLabel.useCachedArtifact.enabled }}
        image: "{{ .Values.modelServers.toxicityIntentMultiLabel.useCachedArtifact.dockerImageOverride.registry }}/{{ .Values.modelServers.toxicityIntentMultiLabel.useCachedArtifact.dockerImageOverride.name }}"
        imagePullPolicy: {{ .Values.modelServers.toxicityIntentMultiLabel.useCachedArtifact.dockerImageOverride.pullPolicy }}
      {{- else }}
        image: "{{ .Values.images.modelServerImage.registry}}/{{ .Values.images.modelServerImage.name }}"
        imagePullPolicy: {{ .Values.images.modelServerImage.pullPolicy }}
      {{- end }}
        env:
        {{- if not .Values.modelServers.toxicityIntentMultiLabel.useCachedArtifact.enabled }}
          - name: HUGGINGFACE_API_KEY
            valueFrom:
              secretKeyRef:
                name: {{ .Values.secrets.existingIntegrationSecretsName }}
                key: huggingfaceAPIKey
        {{- end }}
        ports:
          - name: rest
            protocol: TCP
            containerPort: 8080
        command:
          - "run_server"
        args:
          - "--model-config-path=/model_server/model-settings.json"
          - "--cache-dir=/model_cache"
        startupProbe:
          httpGet:
            path: /v2/models/toxicity-intent-multi-label/ready
            port: 8080
          failureThreshold: 20
          periodSeconds: 5
        readinessProbe:
          httpGet:
            path: /v2/models/toxicity-intent-multi-label/ready
            port: 8080
          periodSeconds: 5
        resources:
          requests:
            memory: {{ .Values.modelServers.toxicityIntentMultiLabel.resources.requests.memory }}
            cpu: {{ .Values.modelServers.toxicityIntentMultiLabel.resources.requests.cpu }}
            {{- if gt (int .Values.modelServers.toxicityIntentMultiLabel.resources.requests.gpu) 0 }}
            nvidia.com/gpu: {{ .Values.modelServers.toxicityIntentMultiLabel.resources.requests.gpu }}
            {{- end }}
          limits:
            memory: {{ .Values.modelServers.toxicityIntentMultiLabel.resources.limits.memory }}
            cpu: {{ .Values.modelServers.toxicityIntentMultiLabel.resources.limits.cpu }}
            {{- if gt (int .Values.modelServers.toxicityIntentMultiLabel.resources.limits.gpu) 0 }}
            nvidia.com/gpu: {{ .Values.modelServers.toxicityIntentMultiLabel.resources.limits.gpu }}
            {{- end }}
        volumeMounts:
          - name: toxicity-intent-multi-label-config
            mountPath: "/model_server"
            readOnly: true
          - name: toxicity-intent-multi-label-model-cache
            mountPath: "/model_cache"
            readOnly: false
    {{- if gt (int .Values.modelServers.toxicityIntentMultiLabel.resources.limits.gpu) 0 }}
    tolerations:
    - effect: NoSchedule
      key: dedicated
      operator: Equal
      value: gpu-dedicated
    {{- else }}
    {{- with .Values.modelServers.deployment.tolerations }}
    tolerations:
      {{- toYaml . | nindent 12 }}
    {{- end }}
    {{- end }}

    topologySpreadConstraints:
    - maxSkew: 1
      topologyKey: "topology.kubernetes.io/zone"
      whenUnsatisfiable: ScheduleAnyway
      labelSelector:
        matchLabels:
          app: toxicityIntentMultiLabel

    affinity:
      podAntiAffinity:
        preferredDuringSchedulingIgnoredDuringExecution:
        - podAffinityTerm:
            labelSelector:
              matchLabels:
                app: toxicityIntentMultiLabel
            topologyKey: topology.kubernetes.io/zone
          weight: 100
        - podAffinityTerm:
            labelSelector:
              matchLabels:
                app: toxicityIntentMultiLabel
            topologyKey: kubernetes.io/hostname
          weight: 99
    volumes:
      # Volumes are defined at the Pod level, then mounted into containers within that Pod
      - name: toxicity-intent-multi-label-config
        configMap:
          name: {{ include "ri-detection-resources.fullname" . }}-toxicity-intent-multi-label-conf
          items:
            - key: "model-settings.json"
              path: "model-settings.json"
      - name: toxicity-intent-multi-label-model-cache
        emptyDir: { }
{{- end }}
