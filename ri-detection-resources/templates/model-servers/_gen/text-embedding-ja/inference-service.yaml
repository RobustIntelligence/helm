{{- if and .Values.modelServers.textEmbeddingJa.enabled (not .Values.modelServers.textEmbeddingJa.remoteModelServer.enabled) }}
# Autogenerated by a script. DO NOT EDIT.
apiVersion: serving.kserve.io/v1beta1
kind: InferenceService
metadata:
  name: text-embedding-ja
  annotations:
    {{- include "ri-detection-resources.annotations" . | nindent 4 }}
spec:
  predictor:
    {{- with .Values.images.imagePullSecrets }}
    imagePullSecrets:
      {{- toYaml . | nindent 8 }}
    {{- end }}
    minReplicas: {{ .Values.modelServers.textEmbeddingJa.minReplicas }}
    maxReplicas: {{ .Values.modelServers.textEmbeddingJa.maxReplicas }}
    securityContext:
      runAsUser: 1000
      runAsGroup: 1000
      fsGroup: 1000
    containers:
      - name: text-embedding-ja
      {{- if .Values.modelServers.textEmbeddingJa.useCachedArtifact.enabled }}
        image: "{{ .Values.modelServers.textEmbeddingJa.useCachedArtifact.dockerImageOverride.registry }}/{{ .Values.modelServers.textEmbeddingJa.useCachedArtifact.dockerImageOverride.name }}"
        imagePullPolicy: {{ .Values.modelServers.textEmbeddingJa.useCachedArtifact.dockerImageOverride.pullPolicy }}
      {{- else }}
        image: "{{ .Values.images.modelServerImage.registry}}/{{ .Values.images.modelServerImage.name }}"
        imagePullPolicy: {{ .Values.images.modelServerImage.pullPolicy }}
      {{- end }}
        env:
        {{- if not .Values.modelServers.textEmbeddingJa.useCachedArtifact.enabled }}
          - name: HUGGINGFACE_API_KEY
            valueFrom:
              secretKeyRef:
                name: {{ .Values.secrets.existingIntegrationSecretsName }}
                key: huggingfaceAPIKey
        {{- end }}
        ports:
          - name: rest
            protocol: TCP
            containerPort: 8080
        command:
          - "run_server"
        args:
          - "--model-config-path=/model_server/model-settings.json"
          - "--cache-dir=/model_cache"
        startupProbe:
          httpGet:
            path: /v2/models/text-embedding-ja/ready
            port: 8080
          failureThreshold: 20
          periodSeconds: 5
        readinessProbe:
          httpGet:
            path: /v2/models/text-embedding-ja/ready
            port: 8080
          periodSeconds: 5
        resources:
          requests:
            memory: {{ .Values.modelServers.textEmbeddingJa.resources.requests.memory }}
            cpu: {{ .Values.modelServers.textEmbeddingJa.resources.requests.cpu }}
          limits:
            memory: {{ .Values.modelServers.textEmbeddingJa.resources.limits.memory }}
            cpu: {{ .Values.modelServers.textEmbeddingJa.resources.limits.cpu }}
        volumeMounts:
          - name: text-embedding-ja-config
            mountPath: "/model_server"
            readOnly: true
          - name: text-embedding-ja-model-cache
            mountPath: "/model_cache"
            readOnly: false
    volumes:
      # Volumes are defined at the Pod level, then mounted into containers within that Pod
      - name: text-embedding-ja-config
        configMap:
          name: {{ include "ri-detection-resources.fullname" . }}-text-embedding-ja-conf
          items:
            - key: "model-settings.json"
              path: "model-settings.json"
      - name: text-embedding-ja-model-cache
        emptyDir: { }
{{- end }}
