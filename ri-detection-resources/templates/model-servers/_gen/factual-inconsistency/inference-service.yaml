{{- if and .Values.modelServers.factualInconsistency.enabled (not .Values.modelServers.factualInconsistency.remoteModelServer.enabled) }}
# Autogenerated by a script. DO NOT EDIT.
apiVersion: serving.kserve.io/v1beta1
kind: InferenceService
metadata:
  name: factual-inconsistency
  annotations:
    {{- include "ri-detection-resources.annotations" . | nindent 4 }}
spec:
  predictor:
    {{- with .Values.images.imagePullSecrets }}
    imagePullSecrets:
      {{- toYaml . | nindent 8 }}
    {{- end }}
    minReplicas: {{ .Values.modelServers.factualInconsistency.minReplicas }}
    maxReplicas: {{ .Values.modelServers.factualInconsistency.maxReplicas }}
    containers:
      - name: factual-inconsistency
      {{- if .Values.modelServers.factualInconsistency.useCachedArtifact.enabled }}
        image: "{{ .Values.modelServers.factualInconsistency.useCachedArtifact.dockerImageOverride.registry }}/{{ .Values.modelServers.factualInconsistency.useCachedArtifact.dockerImageOverride.name }}"
        imagePullPolicy: {{ .Values.modelServers.factualInconsistency.useCachedArtifact.dockerImageOverride.pullPolicy }}
      {{- else }}
        image: "{{ .Values.images.modelServerImage.registry}}/{{ .Values.images.modelServerImage.name }}"
        imagePullPolicy: {{ .Values.images.modelServerImage.pullPolicy }}
      {{- end }}
        env:
        {{- if not .Values.modelServers.factualInconsistency.useCachedArtifact.enabled }}
          - name: HUGGINGFACE_API_KEY
            valueFrom:
              secretKeyRef:
                name: {{ .Values.secrets.existingIntegrationSecretsName }}
                key: huggingfaceAPIKey
        {{- end }}
        ports:
          - name: rest
            protocol: TCP
            containerPort: 8080
        command:
          - "run_server"
        args:
          - "--model-config-path=/model_server/model-settings.json"
          - "--cache-dir=/model_cache"
        startupProbe:
          httpGet:
            path: /v2/models/factual-inconsistency/ready
            port: 8080
          failureThreshold: 20
          periodSeconds: 5
        readinessProbe:
          httpGet:
            path: /v2/models/factual-inconsistency/ready
            port: 8080
          periodSeconds: 5
        resources:
          requests:
            memory: {{ .Values.modelServers.factualInconsistency.resources.requests.memory }}
            cpu: {{ .Values.modelServers.factualInconsistency.resources.requests.cpu }}
          limits:
            memory: {{ .Values.modelServers.factualInconsistency.resources.limits.memory }}
            cpu: {{ .Values.modelServers.factualInconsistency.resources.limits.cpu }}
        volumeMounts:
          - name: factual-inconsistency-config
            mountPath: "/model_server"
            readOnly: true
          - name: factual-inconsistency-model-cache
            mountPath: "/model_cache"
            readOnly: false
    volumes:
      # Volumes are defined at the Pod level, then mounted into containers within that Pod
      - name: factual-inconsistency-config
        configMap:
          name: {{ include "ri-detection-resources.fullname" . }}-factual-inconsistency-conf
          items:
            - key: "model-settings.json"
              path: "model-settings.json"
      - name: factual-inconsistency-model-cache
        emptyDir: { }
{{- end }}
