{{- if .Values.riFirewall.authServer.hpa.enabled }}
apiVersion: {{ include "ri-firewall.hpa.apiVersion" . }}
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "ri-firewall.fullname" . }}-{{ .Values.riFirewall.authServer.name }}
  labels:
    app: {{ .Values.riFirewall.authServer.name }}
    {{- include "ri-firewall.labels" . | nindent 4 }}
    {{- with .Values.riFirewall.authServer.hpa.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "ri-firewall.fullname" . }}-{{ .Values.riFirewall.authServer.name }}
  minReplicas: {{ .Values.riFirewall.authServer.hpa.minReplicas }}
  maxReplicas: {{ .Values.riFirewall.authServer.hpa.maxReplicas }}
  metrics:
{{ toYaml .Values.riFirewall.authServer.hpa.metrics | indent 4 }}
{{- end }}
