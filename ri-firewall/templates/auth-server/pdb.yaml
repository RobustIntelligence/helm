{{- if .Values.riFirewall.authServer.pdb.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "ri-firewall.fullname" . }}-{{ .Values.riFirewall.authServer.name }}
spec:
  minAvailable: {{ .Values.riFirewall.authServer.pdb.minAvailable }}
  selector:
    matchLabels:
      app: {{ .Values.riFirewall.authServer.name }}
{{ end }}
