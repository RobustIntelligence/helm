{{- if .Values.riFirewall.operator.hpa.enabled }}
apiVersion: {{ include "ri-firewall.hpa.apiVersion" . }}
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "ri-firewall.fullname" . }}-{{ .Values.riFirewall.operator.name }}
  labels:
    app: {{ .Values.riFirewall.operator.name }}
    {{- include "ri-firewall.labels" . | nindent 4 }}
    {{- with .Values.riFirewall.operator.hpa.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "ri-firewall.fullname" . }}-{{ .Values.riFirewall.operator.name }}
  minReplicas: {{ .Values.riFirewall.operator.hpa.minReplicas }}
  maxReplicas: {{ .Values.riFirewall.operator.hpa.maxReplicas }}
  metrics:
{{ toYaml .Values.riFirewall.operator.hpa.metrics | indent 4 }}
{{- end }}
