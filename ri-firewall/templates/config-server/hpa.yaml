{{- if .Values.riFirewall.configServer.hpa.enabled }}
apiVersion: {{ include "ri-firewall.hpa.apiVersion" . }}
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "ri-firewall.fullname" . }}-{{ .Values.riFirewall.configServer.name }}
  labels:
    app: {{ .Values.riFirewall.configServer.name }}
    {{- include "ri-firewall.labels" . | nindent 4 }}
    {{- with .Values.riFirewall.configServer.hpa.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "ri-firewall.fullname" . }}-{{ .Values.riFirewall.configServer.name }}
  minReplicas: {{ .Values.riFirewall.configServer.hpa.minReplicas }}
  maxReplicas: {{ .Values.riFirewall.configServer.hpa.maxReplicas }}
  metrics:
{{ toYaml .Values.riFirewall.configServer.hpa.metrics | indent 4 }}
{{- end }}
