{{- if .Values.riFirewall.configServer.serviceAccount.create }}
# Binds Config server service account to a role providing write access to
# k8s resources.
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "ri-firewall.configServer.serviceAccountName" . }}-rolebinding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "ri-firewall.configServer.serviceAccountName" . }}
subjects:
  - kind: ServiceAccount
    name: {{ include "ri-firewall.configServer.serviceAccountName" . }}
{{- end }}
