{{- if .Values.riFirewall.yaraOperator.hpa.enabled }}
apiVersion: {{ include "ri-firewall.hpa.apiVersion" . }}
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "ri-firewall.fullname" . }}-{{ .Values.riFirewall.yaraOperator.name }}
  labels:
    app: {{ .Values.riFirewall.yaraOperator.name }}
    {{- include "ri-firewall.labels" . | nindent 4 }}
    {{- with .Values.riFirewall.yaraOperator.hpa.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "ri-firewall.name" . }}-{{ .Values.riFirewall.yaraOperator.name }}
  minReplicas: {{ .Values.riFirewall.yaraOperator.hpa.minReplicas }}
  maxReplicas: {{ .Values.riFirewall.yaraOperator.hpa.maxReplicas }}
  metrics:
{{ toYaml .Values.riFirewall.yaraOperator.hpa.metrics | indent 4 }}
{{- end }}
