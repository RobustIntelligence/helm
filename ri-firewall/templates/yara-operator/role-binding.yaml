apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "ri-firewall.fullname" . }}-yara-operator-rolebinding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "ri-firewall.fullname" . }}-{{ .Values.riFirewall.yaraOperator.name }}-role
subjects:
- kind: ServiceAccount
  name: {{ include "ri-firewall.yaraOperator.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
