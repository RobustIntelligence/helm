{{- if .Values.riFirewall.yaraServer.hpa.enabled }}
apiVersion: {{ include "ri-firewall.hpa.apiVersion" . }}
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "ri-firewall.fullname" . }}-{{ .Values.riFirewall.yaraServer.name }}
  labels:
    app: {{ .Values.riFirewall.yaraServer.name }}
    {{- include "ri-firewall.labels" . | nindent 4 }}
    {{- with .Values.riFirewall.yaraServer.hpa.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "ri-firewall.fullname" . }}-{{ .Values.riFirewall.yaraServer.name }}
  minReplicas: {{ .Values.riFirewall.yaraServer.hpa.minReplicas }}
  maxReplicas: {{ .Values.riFirewall.yaraServer.hpa.maxReplicas }}
  metrics:
{{ toYaml .Values.riFirewall.yaraServer.hpa.metrics | indent 4 }}
{{- end }}
