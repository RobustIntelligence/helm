{{- if .Values.riFirewall.fileStorageServer.hpa.enabled }}
apiVersion: {{ include "ri-firewall.hpa.apiVersion" . }}
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "ri-firewall.fullname" . }}-{{ .Values.riFirewall.fileStorageServer.name }}
  labels:
    app: {{ .Values.riFirewall.fileStorageServer.name }}
    {{- include "ri-firewall.labels" . | nindent 4 }}
    {{- with .Values.riFirewall.fileStorageServer.hpa.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "ri-firewall.fullname" . }}-{{ .Values.riFirewall.fileStorageServer.name }}
  minReplicas: {{ .Values.riFirewall.fileStorageServer.hpa.minReplicas }}
  maxReplicas: {{ .Values.riFirewall.fileStorageServer.hpa.maxReplicas }}
  metrics:
{{ toYaml .Values.riFirewall.fileStorageServer.hpa.metrics | indent 4 }}
{{- end }}
