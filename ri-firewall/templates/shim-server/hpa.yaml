{{- if and .Values.riFirewall.shimServer.enabled .Values.riFirewall.shimServer.hpa.enabled }}
apiVersion: {{ include "ri-firewall.hpa.apiVersion" . }}
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "ri-firewall.fullname" . }}-{{ .Values.riFirewall.shimServer.name }}
  labels:
    app: {{ .Values.riFirewall.shimServer.name }}
    {{- include "ri-firewall.labels" . | nindent 4 }}
    {{- with .Values.riFirewall.shimServer.hpa.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "ri-firewall.fullname" . }}-{{ .Values.riFirewall.shimServer.name }}
  minReplicas: {{ .Values.riFirewall.shimServer.hpa.minReplicas }}
  maxReplicas: {{ .Values.riFirewall.shimServer.hpa.maxReplicas }}
  metrics:
{{ toYaml .Values.riFirewall.shimServer.hpa.metrics | indent 4 }}
{{- end }}
