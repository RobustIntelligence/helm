{{- if .Values.riFirewall.instanceManagerServer.hpa.enabled }}
apiVersion: {{ include "ri-firewall.hpa.apiVersion" . }}
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "ri-firewall.fullname" . }}-{{ .Values.riFirewall.instanceManagerServer.name }}
  labels:
    app: {{ .Values.riFirewall.instanceManagerServer.name }}
    {{- include "ri-firewall.labels" . | nindent 4 }}
    {{- with .Values.riFirewall.instanceManagerServer.hpa.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "ri-firewall.fullname" . }}-{{ .Values.riFirewall.instanceManagerServer.name }}
  minReplicas: {{ .Values.riFirewall.instanceManagerServer.hpa.minReplicas }}
  maxReplicas: {{ .Values.riFirewall.instanceManagerServer.hpa.maxReplicas }}
  metrics:
{{ toYaml .Values.riFirewall.instanceManagerServer.hpa.metrics | indent 4 }}
{{- end }}
