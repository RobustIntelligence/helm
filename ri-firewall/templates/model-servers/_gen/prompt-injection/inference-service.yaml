# Autogenerated by a script. DO NOT EDIT.
apiVersion: serving.kserve.io/v1beta1
kind: InferenceService
metadata:
  name: prompt-injection
  annotations:
    {{- include "ri-firewall.annotations" . | nindent 4 }}
spec:
  predictor:
    {{- with .Values.riFirewall.images.imagePullSecrets }}
    imagePullSecrets:
      {{- toYaml . | nindent 8 }}
    {{- end }}
    minReplicas: {{ .Values.riFirewall.modelServers.promptInjection.minReplicas }}
    maxReplicas: {{ .Values.riFirewall.modelServers.promptInjection.maxReplicas }}
    containers:
      - name: prompt-injection
        image: "{{ .Values.riFirewall.images.modelServerImage.registry}}/{{ .Values.riFirewall.images.modelServerImage.name }}"
        imagePullPolicy: {{ .Values.riFirewall.images.modelServerImage.pullPolicy }}
        env:
          - name: HUGGINGFACE_API_KEY
            valueFrom:
              secretKeyRef:
                name: {{ .Values.riFirewall.secrets.existingIntegrationSecretsName }}
                key: huggingfaceAPIKey
        ports:
          - name: rest
            protocol: TCP
            containerPort: 8080
        command:
          - "run_server"
        args:
          - "--model-config-path={{ .Values.riFirewall.modelServers.modelSettingsPath }}/model-settings.json"
        resources:
          requests:
            memory: {{ .Values.riFirewall.modelServers.promptInjection.resources.requests.memory }}
            cpu: {{ .Values.riFirewall.modelServers.promptInjection.resources.requests.cpu }}
          limits:
            memory: {{ .Values.riFirewall.modelServers.promptInjection.resources.limits.memory }}
            cpu: {{ .Values.riFirewall.modelServers.promptInjection.resources.limits.cpu }}
        volumeMounts:
          - name: {{ include "ri-firewall.fullname" . }}-prompt-injection-config
            mountPath: {{ .Values.riFirewall.modelServers.modelSettingsPath }}
            readOnly: true
    volumes:
      # Volumes are defined at the Pod level, then mounted into containers within that Pod
      - name: {{ include "ri-firewall.fullname" . }}-prompt-injection-config
        configMap:
          name: {{ include "ri-firewall.fullname" . }}-prompt-injection-conf
          items:
            - key: "model-settings.json"
              path: "model-settings.json"
