---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.3
  name: yaraservers.fw.rbst.io
spec:
  group: fw.rbst.io
  names:
    kind: YaraServer
    listKind: YaraServerList
    plural: yaraservers
    singular: yaraserver
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.description
      name: Description
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: YaraServer is the Schema for the yaraserver API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              YaraServerSpec defines the desired state of YaraServer.
              K8s is a declarative API; the operator will reconcile the state on the
              cluster step-by-step until the actual state reaches the desired state
              specified here.
              The desired state of the YaraService is a firewall deployment with the
              specified rule configuration.
            properties:
              kubernetesSpec:
                description: |-
                  Optional kubernetes settings for the FirewallInstance.
                  Values already have defaults set by the underlying resource templates.
                properties:
                  autoscalingOverrides:
                    description: |-
                      AutoscalingConfig is configuration for the autoscaling of the YARA
                      server.
                      This is an override for Helm-deployment-wide defaults.
                    properties:
                      maxReplicasOverride:
                        description: |-
                          MaxReplicasOverride is the override for the maximum number of YARA server
                          replicas.
                          If not specified, the default set by Helm chart values will be used.
                        format: int32
                        minimum: 0
                        type: integer
                      minReplicasOverride:
                        description: |-
                          MinReplicasOverride is the override for the minimum number of YARA server
                          replicas.
                          If not specified, the default set by Helm chart values will be used.
                        format: int32
                        minimum: 0
                        type: integer
                    type: object
                  podAnnotations:
                    additionalProperties:
                      type: string
                    description: |-
                      PodAnnotations are tags that are propagated to all the YARA server
                      pods for this deployment.
                    type: object
                  yaraServerResourceOverrides:
                    description: |-
                      YaraServerResourceOverrides is an override for the default
                      resource requirements for the `firewall-server` container.
                    properties:
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info:
                          https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is
                          explicitly specified, otherwise to an implementation-defined value.
                          Requests cannot exceed Limits. More info:
                          https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                type: object
              versionConfig:
                description: |-
                  VersionConfig contains the versions of the YARA signatures that
                  should be used.
                properties:
                  image:
                    description: Docker image to use for the deployment.
                    type: string
                  patternRepoRef:
                    description: |-
                      PatternRepoRef is the git ref of the YARA signatures repository to
                      use.
                    type: string
                type: object
            type: object
          status:
            description: YaraServerStatus defines the observed state of YaraServer.
            properties:
              conditions:
                description: Represents the observations of a YaraServer current state.
                items:
                  description: |-
                    YaraServerCondition describes a single condition for the YaraServer status.
                    This follows the common conventions for representing status in K8s.
                    https://github.com/kubernetes/community/blob/master/contributors/devel/sig-architecture/api-conventions.md#typical-status-properties.
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      format: date-time
                      type: string
                    lastUpdateTime:
                      description: The last time this condition was updated.
                      format: date-time
                      type: string
                    message:
                      description: A human-readable message indicating details about
                        the transition.
                      type: string
                    reason:
                      description: The reason for the condition's last transition.
                      type: string
                    status:
                      description: Status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: Type of the YaraServer condition.
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
