{{- if .Values.aiValidation.detectionServer.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ai-validation.fullname" . }}-{{ .Values.aiValidation.detectionServer.name }}-system-config
data:
  systemConfig: |
    yara_server_address: ""
{{- end }}
