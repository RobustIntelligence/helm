{{- if and .Values.rime.datasetManagerServer.enabled .Values.rime.datasetManagerServer.hpa.enabled }}
apiVersion: {{ include "rime.hpa.apiVersion" . }}
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "rime.fullname" . }}-{{ .Values.rime.datasetManagerServer.name }}
  labels:
    app: {{ .Values.rime.datasetManagerServer.name }}
    {{- include "rime.labels" . | nindent 4 }}
    {{- with .Values.rime.datasetManagerServer.hpa.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "rime.fullname" . }}-{{ .Values.rime.datasetManagerServer.name }}
  minReplicas: {{ .Values.rime.datasetManagerServer.hpa.minReplicas }}
  maxReplicas: {{ .Values.rime.datasetManagerServer.hpa.maxReplicas }}
  metrics:
{{ toYaml .Values.rime.datasetManagerServer.hpa.metrics | indent 4 }}
{{- end }}
