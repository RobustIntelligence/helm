{{- if eq .Values.rime.notificationsWorker.existingConfigSecretName "" }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "rime.fullname" . }}-{{ .Values.rime.notificationsWorker.name }}-conf
data:
  server.config: |
{{ include "rime.serverArgs" . | indent 4 }}
{{ include "rime.notificationsWorker.serverArgs" . | indent 4 }}
{{- end }}
