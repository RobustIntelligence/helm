{{- if .Values.rime.firewallServer.hpa.enabled }}
apiVersion: {{ include "rime.hpa.apiVersion" . }}
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "rime.fullname" . }}-{{ .Values.rime.firewallServer.name }}
  labels:
    app: {{ .Values.rime.firewallServer.name }}
    {{- include "rime.labels" . | nindent 4 }}
    {{- with .Values.rime.firewallServer.hpa.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "rime.fullname" . }}-{{ .Values.rime.firewallServer.name }}
  minReplicas: {{ .Values.rime.firewallServer.hpa.minReplicas }}
  maxReplicas: {{ .Values.rime.firewallServer.hpa.maxReplicas }}
  metrics:
{{ toYaml .Values.rime.firewallServer.hpa.metrics | indent 4 }}
{{- end }}
