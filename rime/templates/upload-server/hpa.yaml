{{- if .Values.rime.uploadServer.hpa.enabled }}
apiVersion: {{ include "rime.hpa.apiVersion" . }}
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "rime.fullname" . }}-{{ .Values.rime.uploadServer.name }}
  labels:
    app: {{ .Values.rime.uploadServer.name }}
    {{- include "rime.labels" . | nindent 4 }}
    {{- with .Values.rime.uploadServer.hpa.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "rime.fullname" . }}-{{ .Values.rime.uploadServer.name }}
  minReplicas: {{ .Values.rime.uploadServer.hpa.minReplicas }}
  maxReplicas: {{ .Values.rime.uploadServer.hpa.maxReplicas }}
  metrics:
{{ toYaml .Values.rime.uploadServer.hpa.metrics | indent 4 }}
{{- end }}
