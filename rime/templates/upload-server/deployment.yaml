apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "rime.fullname" . }}-{{ .Values.rime.uploadServer.name }}
  labels:
    app: {{ .Values.rime.uploadServer.name }}
    {{- include "rime.labels" . | nindent 4 }}
    {{- with .Values.rime.uploadServer.deployment.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  annotations:
    {{- include "rime.annotations" . | nindent 4 }}
    {{- with .Values.rime.uploadServer.deployment.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  selector:
    matchLabels:
      app: {{ .Values.rime.uploadServer.name }}
      {{- include "rime.selectorLabels" . | nindent 6 }}
  {{- if not .Values.rime.uploadServer.hpa.enabled }}
  replicas: {{ .Values.rime.uploadServer.deployment.replicaCount }}
  {{- end }}
  template:
    metadata:
      labels:
        app: {{ .Values.rime.uploadServer.name }}
        {{- include "rime.labels" . | nindent 8 }}
        {{- with .Values.rime.uploadServer.deployment.labels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      annotations:
        {{- include "rime.annotations" . | nindent 8 }}
        {{- with .Values.rime.uploadServer.deployment.annotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- include "rime.monitoringAnnotations" (dict "monitoring" .Values.rime.monitoring "name" .Values.rime.uploadServer.name ) | nindent 8 }}
        checksum/config: {{ include (print $.Template.BasePath "/upload-server/configmap.yaml") . | sha256sum }}
    spec:
      {{- with .Values.rime.images.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.rime.uploadServer.deployment.securityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "rime.uploadServer.serviceAccountName" . }}
      containers:
        - name: {{ .Values.rime.uploadServer.name }}
          env:
            {{- include "rime.webAppHostEnv" . | nindent 12 }}
            {{- include "rime.commonEnv" . | nindent 12 }}
            {{- with .Values.rime.uploadServer.deployment.extraEnv }}
              {{- toYaml . | nindent 12 }}
            {{- end }}
          image: "{{ .Values.rime.images.backendImage.registry}}/{{ .Values.rime.images.backendImage.name }}"
          imagePullPolicy: {{ .Values.rime.images.backendImage.pullPolicy }}
          ports:
            - name: us
              containerPort: {{ .Values.rime.uploadServer.port }}
              protocol: TCP
            - name: us-rest
              containerPort: {{ .Values.rime.uploadServer.restPort }}
              protocol: TCP
            - name: us-debug
              containerPort: 6060
              protocol: TCP
            {{- if .Values.rime.monitoring.enabled }}
            - name: metrics
              containerPort: {{ .Values.rime.monitoring.port }}
              protocol: TCP
            {{- end }}
          {{ if .Values.tls.grpcEnabled }}
          livenessProbe:
            exec:
              command: [ "/bin/grpc_health_probe", "-tls", "-tls-no-verify", "-addr=:{{ .Values.rime.uploadServer.port }}" ]
            periodSeconds: 5
            timeoutSeconds: 5
          readinessProbe:
            exec:
              command: [ "/bin/grpc_health_probe", "-tls", "-tls-no-verify", "-addr=:{{ .Values.rime.uploadServer.port }}" ]
            periodSeconds: 5
            timeoutSeconds: 5
          startupProbe:
            exec:
              command: [ "/bin/grpc_health_probe", "-tls", "-tls-no-verify", "-addr=:{{ .Values.rime.uploadServer.port }}" ]
            failureThreshold: 60
            periodSeconds: 5
          {{ else }}
          livenessProbe:
            exec:
              command: [ "/bin/grpc_health_probe", "-addr=:{{ .Values.rime.uploadServer.port }}" ]
            periodSeconds: 5
          readinessProbe:
            exec:
              command: [ "/bin/grpc_health_probe", "-addr=:{{ .Values.rime.uploadServer.port }}" ]
            periodSeconds: 5
          startupProbe:
            exec:
              command: [ "/bin/grpc_health_probe", "-addr=:{{ .Values.rime.uploadServer.port }}" ]
            failureThreshold: 60
            periodSeconds: 5
          {{ end }}
          resources:
            {{- toYaml .Values.rime.uploadServer.deployment.resources | nindent 12 }}
          command:
            - "/rime/rime"
          args:
            - "start-upload-server"
            - "--server-config-path=/config/server.config"
            - "--upload-port={{ .Values.rime.uploadServer.port }}"
          volumeMounts:
            - name: {{ include "rime.fullname" . }}-{{ .Values.rime.uploadServer.name }}-config
              mountPath: "/config"
              readOnly: true
            {{- if .Values.tls.enableCertManager }}
            - name: {{ include "rime.fullname" . }}-{{ .Values.rime.uploadServer.name }}-tls
              mountPath: /var/tmp/tls/common
              readOnly: true
            {{- end }}
            {{- include "rime.externalTLSSecretVolumeMounts" . | nindent 12 }}
            {{- with .Values.rime.uploadServer.deployment.extraVolumeMounts }}
              {{- toYaml . | nindent 12 }}
            {{- end }}
      volumes:
        # Volumes are defined at the Pod level, then mounted into containers within that Pod
        - name: {{ include "rime.fullname" . }}-{{ .Values.rime.uploadServer.name }}-config
          {{- if ne .Values.rime.uploadServer.existingConfigSecretName "" }}
          secret:
            secretName: {{ .Values.rime.uploadServer.existingConfigSecretName }}
          {{- else }}
          configMap:
            name: {{ include "rime.fullname" . }}-{{ .Values.rime.uploadServer.name }}-conf
          {{- end }}
            items:
              - key: "server.config"
                path: "server.config"
        {{- if .Values.tls.enableCertManager }}
        - name: {{ include "rime.fullname" . }}-{{ .Values.rime.uploadServer.name }}-tls
          secret:
            secretName: {{ include "rime.fullname" . }}-{{ .Values.rime.uploadServer.name }}-tls
        {{- end }}
        {{- include "rime.externalTLSSecretVolumes" . | nindent 8 }}
        {{- with .Values.rime.uploadServer.deployment.extraVolumes }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- with .Values.rime.uploadServer.deployment.tolerations }}
      tolerations:
        {{- toYaml . | nindent 12 }}
      {{- end }}
      {{- with .Values.rime.uploadServer.deployment.affinity }}
      affinity:
        {{- toYaml . | nindent 12 }}
      {{- end }}
      {{- with .Values.rime.uploadServer.deployment.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 12 }}
      {{- end }}
