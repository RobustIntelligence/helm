{{- if .Values.rime.storageManagerServer.hpa.enabled }}
apiVersion: {{ include "rime.hpa.apiVersion" . }}
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "rime.fullname" . }}-{{ .Values.rime.storageManagerServer.name }}
  labels:
    app: {{ .Values.rime.storageManagerServer.name }}
    {{- include "rime.labels" . | nindent 4 }}
    {{- with .Values.rime.storageManagerServer.hpa.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: StatefulSet
    name: {{ include "rime.fullname" . }}-{{ .Values.rime.storageManagerServer.name }}
  minReplicas: {{ .Values.rime.storageManagerServer.hpa.minReplicas }}
  maxReplicas: {{ .Values.rime.storageManagerServer.hpa.maxReplicas }}
  metrics:
{{ toYaml .Values.rime.storageManagerServer.hpa.metrics | indent 4 }}
{{- end }}
