{{- if .Values.rime.modelTestingServer.hpa.enabled }}
apiVersion: {{ include "rime.hpa.apiVersion" . }}
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "rime.fullname" . }}-{{ .Values.rime.modelTestingServer.name }}
  labels:
    app: {{ .Values.rime.modelTestingServer.name }}
    {{- include "rime.labels" . | nindent 4 }}
    {{- with .Values.rime.modelTestingServer.hpa.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "rime.fullname" . }}-{{ .Values.rime.modelTestingServer.name }}
  minReplicas: {{ .Values.rime.modelTestingServer.hpa.minReplicas }}
  maxReplicas: {{ .Values.rime.modelTestingServer.hpa.maxReplicas }}
  metrics:
{{ toYaml .Values.rime.modelTestingServer.hpa.metrics | indent 4 }}
{{- end }}
