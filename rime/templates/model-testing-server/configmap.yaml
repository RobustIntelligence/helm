{{- if eq .Values.rime.modelTestingServer.existingConfigSecretName "" }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "rime.fullname" . }}-{{ .Values.rime.modelTestingServer.name }}-conf
data:
  server.config: |
{{ include "rime.serverArgs" . | indent 4 }}
{{ include "rime.modelTestingServer.serverArgs" . | indent 4 }}
{{- end }}
