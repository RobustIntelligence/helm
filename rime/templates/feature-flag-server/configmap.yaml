{{- if eq .Values.rime.featureFlagServer.existingConfigSecretName "" }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "rime.fullname" . }}-{{ .Values.rime.featureFlagServer.name }}-conf
data:
  server.config: |
{{ include "rime.serverArgs" . | indent 4 }}
{{ include "rime.featureFlagServer.serverArgs" . | indent 4 }}
{{- end }}
