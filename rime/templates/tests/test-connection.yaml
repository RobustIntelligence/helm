apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rime.fullname" . }}-test-connection"
  labels:
    {{- include "rime.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rime.fullname" . }}:{{ .Values.ports.nativerestweb }}']
  restartPolicy: Never
