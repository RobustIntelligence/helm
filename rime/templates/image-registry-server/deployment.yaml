{{- if .Values.rime.imageRegistryServer.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "rime.fullname" . }}-{{ .Values.rime.imageRegistryServer.name }}
  labels:
    app: {{ .Values.rime.imageRegistryServer.name }}
    {{- include "rime.labels" . | nindent 4 }}
    {{- with .Values.rime.imageRegistryServer.deployment.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  annotations:
    {{- include "rime.annotations" . | nindent 4 }}
    {{- with .Values.rime.imageRegistryServer.deployment.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  selector:
    matchLabels:
      app: {{ .Values.rime.imageRegistryServer.name }}
      {{- include "rime.selectorLabels" . | nindent 6 }}
  {{- if not .Values.rime.imageRegistryServer.hpa.enabled }}
  replicas: {{ .Values.rime.imageRegistryServer.deployment.replicaCount }}
  {{- end }}
  template:
    metadata:
      labels:
        app: {{ .Values.rime.imageRegistryServer.name }}
        {{- include "rime.labels" . | nindent 8 }}
        {{- with .Values.rime.imageRegistryServer.deployment.labels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      annotations:
        {{- include "rime.annotations" . | nindent 8 }}
        {{- with .Values.rime.imageRegistryServer.deployment.annotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- include "rime.monitoringAnnotations" (dict "monitoring" .Values.rime.monitoring "name" .Values.rime.uploadServer.name ) | nindent 8 }}
        checksum/config: {{ include (print $.Template.BasePath "/image-registry-server/configmap.yaml") . | sha256sum }}
    spec:
      {{- with .Values.rime.images.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.rime.imageRegistryServer.deployment.securityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "rime.imageRegistryServer.serviceAccountName" . }}
      containers:
        - name: {{ .Values.rime.imageRegistryServer.name }}
          env:
            {{- include "rime.commonEnv" . | nindent 12 }}
            {{- with .Values.rime.imageRegistryServer.deployment.extraEnv }}
              {{- toYaml . | nindent 12 }}
            {{- end }}
            - name: RI_SERVICE_NAME
              value: {{ .Values.rime.imageRegistryServer.name }}
          image: "{{ .Values.rime.images.backendImage.registry}}/{{ .Values.rime.images.backendImage.name }}"
          imagePullPolicy: {{ .Values.rime.images.backendImage.pullPolicy }}
          ports:
            - name: ir
              containerPort: {{ .Values.rime.imageRegistryServer.port }}
              protocol: TCP
            - name: ir-rest
              containerPort: {{ .Values.rime.imageRegistryServer.restPort }}
              protocol: TCP
            - name: ir-debug
              containerPort: 6060
              protocol: TCP
            {{- if .Values.rime.monitoring.enabled }}
            - name: metrics
              containerPort: {{ .Values.rime.monitoring.port }}
              protocol: TCP
            {{- end }}
          {{ if .Values.tls.grpcEnabled }}
          livenessProbe:
            exec:
              command: [ "/bin/grpc_health_probe", "-tls", "-tls-no-verify", "-addr=:{{ .Values.rime.imageRegistryServer.port }}" ]
            periodSeconds: 5
            timeoutSeconds: 5
          readinessProbe:
            exec:
              command: [ "/bin/grpc_health_probe", "-tls", "-tls-no-verify", "-addr=:{{ .Values.rime.imageRegistryServer.port }}" ]
            periodSeconds: 5
            timeoutSeconds: 5
          startupProbe:
            exec:
              command: [ "/bin/grpc_health_probe", "-tls", "-tls-no-verify", "-addr=:{{ .Values.rime.imageRegistryServer.port }}" ]
            failureThreshold: 60
            periodSeconds: 5
          {{ else }}
          livenessProbe:
            exec:
              command: [ "/bin/grpc_health_probe", "-addr=:{{ .Values.rime.imageRegistryServer.port }}" ]
            periodSeconds: 5
          readinessProbe:
            exec:
              command: [ "/bin/grpc_health_probe", "-addr=:{{ .Values.rime.imageRegistryServer.port }}" ]
            periodSeconds: 5
          startupProbe:
            exec:
              command: [ "/bin/grpc_health_probe", "-addr=:{{ .Values.rime.imageRegistryServer.port }}" ]
            failureThreshold: 60
            periodSeconds: 5
          {{ end }}
          resources:
            {{- toYaml .Values.rime.imageRegistryServer.deployment.resources | nindent 12 }}
          command:
            - "/rime/rime"
          args:
            - "start-image-registry-server"
            - "--server-config-path=/config/server.config"
            - "--image-builder-job-configmap-file=/config/image_builder_job_configmap.config"
            - "--image-builder-job-file=/config/image_builder_job.config"
            - "--image-registry-port={{ .Values.rime.imageRegistryServer.port }}"
            - "--image-registry-rest-port={{ .Values.rime.imageRegistryServer.restPort }}"
          volumeMounts:
            - name: {{ include "rime.fullname" . }}-{{ .Values.rime.imageRegistryServer.name }}-config
              mountPath: "/config"
              readOnly: true
            {{- if .Values.tls.enableCertManager }}
            - name: {{ include "rime.fullname" . }}-{{ .Values.rime.imageRegistryServer.name }}-tls
              mountPath: /var/tmp/tls/common
              readOnly: true
            {{- end }}
            {{- include "rime.externalTLSSecretVolumeMounts" . | nindent 12 }}
            {{- with .Values.rime.imageRegistryServer.deployment.extraVolumeMounts }}
              {{- toYaml . | nindent 12 }}
            {{- end }}
      volumes:
        # Volumes are defined at the Pod level, then mounted into containers within that Pod
        - name: {{ include "rime.fullname" . }}-{{ .Values.rime.imageRegistryServer.name }}-config
          configMap:
            name: {{ include "rime.fullname" . }}-{{ .Values.rime.imageRegistryServer.name }}-conf
            items:
              - key: "server.config"
                path: "server.config"
              - key: "image_builder_job_configmap.config"
                path: "image_builder_job_configmap.config"
              - key: "image_builder_job.config"
                path: "image_builder_job.config"
        {{- if .Values.tls.enableCertManager }}
        - name: {{ include "rime.fullname" . }}-{{ .Values.rime.imageRegistryServer.name }}-tls
          secret:
            secretName: {{ include "rime.fullname" . }}-{{ .Values.rime.imageRegistryServer.name }}-tls
        {{- end }}
        {{- include "rime.externalTLSSecretVolumes" . | nindent 8 }}
        {{- with .Values.rime.imageRegistryServer.deployment.extraVolumes }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- with .Values.rime.imageRegistryServer.deployment.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.rime.imageRegistryServer.deployment.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.rime.imageRegistryServer.deployment.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
