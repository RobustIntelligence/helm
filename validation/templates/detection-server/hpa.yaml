{{- if and .Values.aiValidation.detectionServer.enabled .Values.aiValidation.detectionServer.hpa.enabled }}
apiVersion: {{ include "ai-validation.hpa.apiVersion" . }}
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.aiValidation.detectionServer.name }}
  labels:
    app: {{ .Values.aiValidation.detectionServer.name }}
    {{- include "ai-validation.labels" . | nindent 4 }}
    {{- with .Values.aiValidation.detectionServer.hpa.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.aiValidation.detectionServer.name }}
  minReplicas: {{ .Values.aiValidation.detectionServer.hpa.minReplicas }}
  maxReplicas: {{ .Values.aiValidation.detectionServer.hpa.maxReplicas }}
  metrics:
{{ toYaml .Values.aiValidation.detectionServer.hpa.metrics | indent 4 }}
{{- end }}
