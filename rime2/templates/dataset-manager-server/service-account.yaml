{{- if and .Values.rime.datasetManagerServer.blobStore.rbac.create .Values.rime.datasetManagerServer.blobStore.rbac.blobStoreAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "blobStoreAccount.name" . }}
{{- with .Values.rime.datasetManagerServer.blobStore.rbac.blobStoreAccount.annotations }}
  annotations: {{ toYaml . | nindent 4 }}
{{- end }}
{{- end }}
