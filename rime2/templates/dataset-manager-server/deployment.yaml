{{- if .Values.rime.datasetManagerServer.useFileUploadService }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "rime.fullname" . }}-{{.Values.rime.datasetManagerServer.name}}
  labels:
    app: {{.Values.rime.datasetManagerServer.name}}
    {{- include "rime.labels" . | nindent 4 }}
    {{- with .Values.rime.datasetManagerServer.deployment.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  annotations:
    {{- include "rime.annotations" . | nindent 4 }}
    {{- with .Values.rime.datasetManagerServer.deployment.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  selector:
    matchLabels:
      app: {{.Values.rime.datasetManagerServer.name}}
      {{- include "rime.selectorLabels" . | nindent 6 }}
  {{- if not .Values.rime.datasetManagerServer.hpa.enabled }}
  replicas: {{ .Values.rime.datasetManagerServer.deployment.replicaCount }}
  {{- end }}
  template:
    metadata:
      labels:
        app: {{.Values.rime.datasetManagerServer.name}}
        {{- include "rime.labels" . | nindent 8 }}
        {{- with .Values.rime.datasetManagerServer.deployment.labels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      annotations:
        {{- include "rime.annotations" . | nindent 8 }}
        {{- with .Values.rime.datasetManagerServer.deployment.annotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.rime.images.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if .Values.rime.datasetManagerServer.useFileUploadService }}
      serviceAccountName: {{ template "blobStoreAccount.name" . }}
      {{- end }}
      containers:
        - name: {{.Values.rime.datasetManagerServer.name}}
          env:
            {{- include "rime.jwtEnv" . | nindent 12 }}
          image: "{{.Values.rime.images.backendImage.registry}}/{{ .Values.rime.images.backendImage.name }}"
          imagePullPolicy: {{ .Values.rime.images.backendImage.pullPolicy }}
          ports:
            - name: dm
              containerPort: {{ .Values.rime.datasetManagerServer.port }}
              protocol: TCP
            - name: dm-rest
              containerPort: {{ .Values.rime.datasetManagerServer.restPort }}
              protocol: TCP
            - name: dm-debug
              containerPort: 6060
              protocol: TCP
          livenessProbe:
            exec:
              command: [ "/bin/grpc_health_probe", "-addr=:{{ .Values.rime.datasetManagerServer.port }}" ]
            periodSeconds: 5
          readinessProbe:
            exec:
              command: [ "/bin/grpc_health_probe", "-addr=:{{ .Values.rime.datasetManagerServer.port }}" ]
            periodSeconds: 5
          startupProbe:
            exec:
              command: [ "/bin/grpc_health_probe", "-addr=:{{ .Values.rime.datasetManagerServer.port }}" ]
            failureThreshold: 60
            periodSeconds: 5
          resources:
            {{- toYaml .Values.rime.datasetManagerServer.deployment.resources | nindent 12 }}
          command:
            - "/rime/rime"
          args:
            - "start-dataset-manager-server"
            - "--server-config-path=/config/server.config"
            - "--database-url={{ include "rime.fullname" . }}-mongodb-headless/?tls=false"
            - "--database-url-prefix=mongodb+srv://"
            - "--dataset-manager-port={{ .Values.rime.datasetManagerServer.port }}"
            - "--dataset-manager-port={{ .Values.rime.datasetManagerServer.restPort }}"
            - "--blob-store-bucket-name={{ .Values.rime.datasetManagerServer.blobStore.storageBucketName }}"
            - "--blob-store-bucket-endpoint={{ .Values.rime.datasetManagerServer.blobStore.endpoint }}"
            - "--blob-store-service-type={{ .Values.rime.datasetManagerServer.blobStore.type }}"
            {{- if .Values.rime.monitoring.enabled }}
            - "--metrics-exposition-enabled=true"
            - "--metrics-exposition-port={{ .Values.rime.monitoring.port }}"
            {{- end }}
          volumeMounts:
            - name: {{ include "rime.fullname" . }}-{{ .Values.rime.datasetManagerServer.name }}-config
              mountPath: "/config"
              readOnly: true
      volumes:
        # Volumes are defined at the Pod level, then mounted into containers within that Pod
        - name: {{ include "rime.fullname" . }}-{{ .Values.rime.datasetManagerServer.name }}-config
          configMap:
            name: {{ include "rime.fullname" . }}-{{ .Values.rime.datasetManagerServer.name }}-conf
            items:
              - key: "server.config"
                path: "server.config"
      {{- with .Values.rime.datasetManagerServer.deployment.tolerations }}
      tolerations:
        {{- toYaml . | nindent 12 }}
      {{- end }}
      {{- with .Values.rime.datasetManagerServer.deployment.affinity }}
      affinity:
        {{- toYaml . | nindent 12 }}
      {{- end }}
      {{- with .Values.rime.datasetManagerServer.deployment.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 12 }}
      {{- end }}
{{- end }}
