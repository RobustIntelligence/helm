apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "rime.fullname" . }}-{{.Values.rime.modelCardServer.name}}
  labels:
    app: {{.Values.rime.modelCardServer.name}}
    {{- include "rime.labels" . | nindent 4 }}
    {{- with .Values.rime.modelCardServer.deployment.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  annotations:
    {{- include "rime.annotations" . | nindent 4 }}
    {{- with .Values.rime.modelCardServer.deployment.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  selector:
    matchLabels:
      app: {{.Values.rime.modelCardServer.name}}
      {{- include "rime.selectorLabels" . | nindent 6 }}
  {{- if not .Values.rime.modelCardServer.hpa.enabled }}
  replicas: {{ .Values.rime.modelCardServer.deployment.replicaCount }}
  {{- end }}
  template:
    metadata:
      labels:
        app: {{.Values.rime.modelCardServer.name}}
        {{- include "rime.labels" . | nindent 8 }}
        {{- with .Values.rime.modelCardServer.deployment.labels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      annotations:
        {{- include "rime.annotations" . | nindent 8 }}
        {{- with .Values.rime.modelCardServer.deployment.annotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.rime.images.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: {{.Values.rime.modelCardServer.name}}
          env:
            {{- include "rime.jwtEnv" . | nindent 12 }}
          image: "{{.Values.rime.images.backendImage.registry}}/{{ .Values.rime.images.backendImage.name }}"
          imagePullPolicy: {{ .Values.rime.images.backendImage.pullPolicy }}
          ports:
            - name: mc
              containerPort: {{ .Values.rime.modelCardServer.port }}
              protocol: TCP
            - name: mc-rest
              containerPort: {{ .Values.rime.modelCardServer.restPort }}
              protocol: TCP
            - name: mc-debug
              containerPort: 6060
              protocol: TCP
          livenessProbe:
            exec:
              command: [ "/bin/grpc_health_probe", "-tls", "-tls-no-verify", "-addr=:{{ .Values.rime.modelCardServer.port }}" ]
            periodSeconds: 5
          readinessProbe:
            exec:
              command: [ "/bin/grpc_health_probe", "-tls", "-tls-no-verify", "-addr=:{{ .Values.rime.modelCardServer.port }}" ]
            periodSeconds: 5
          startupProbe:
            exec:
              command: [ "/bin/grpc_health_probe", "-tls", "-tls-no-verify", "-addr=:{{ .Values.rime.modelCardServer.port }}" ]
            failureThreshold: 60
            periodSeconds: 5
          resources:
            {{- toYaml .Values.rime.modelCardServer.deployment.resources | nindent 12 }}
          command:
            - "/rime/rime"
          args:
            - "start-model-card-server"
            - "--server-config-path=/config/server.config"
            - "--database-url={{ include "rime.fullname" . }}-mongodb-headless/?tls=false"
            - "--database-url-prefix=mongodb+srv://"
            - "--model-card-port={{ .Values.rime.modelCardServer.port }}"
            - "--model-card-rest-port={{ .Values.rime.modelCardServer.restPort }}"
            {{- if .Values.rime.monitoring.enabled }}
            - "--metrics-exposition-enabled=true"
            - "--metrics-exposition-port={{ .Values.rime.monitoring.port }}"
            {{- end }}
          volumeMounts:
            - name: {{ include "rime.fullname" . }}-{{ .Values.rime.modelCardServer.name }}-config
              mountPath: "/config"
              readOnly: true
      volumes:
        # Volumes are defined at the Pod level, then mounted into containers within that Pod
        - name: {{ include "rime.fullname" . }}-{{ .Values.rime.modelCardServer.name }}-config
          configMap:
            name: {{ include "rime.fullname" . }}-{{ .Values.rime.modelCardServer.name }}-conf
            items:
              - key: "server.config"
                path: "server.config"
      {{- with .Values.rime.modelCardServer.deployment.tolerations }}
      tolerations:
        {{- toYaml . | nindent 12 }}
      {{- end }}
      {{- with .Values.rime.modelCardServer.deployment.affinity }}
      affinity:
        {{- toYaml . | nindent 12 }}
      {{- end }}
      {{- with .Values.rime.modelCardServer.deployment.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 12 }}
      {{- end }}
