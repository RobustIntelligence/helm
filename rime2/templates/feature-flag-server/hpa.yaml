{{- if .Values.rime.featureFlagServer.hpa.enabled }}
apiVersion: autoscaling/v2beta1
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "rime.fullname" . }}-{{.Values.rime.featureFlagServer.name}}
  labels:
    app: {{.Values.rime.featureFlagServer.name}}
    {{- include "rime.labels" . | nindent 4 }}
    {{- with .Values.rime.featureFlagServer.hpa.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "rime.fullname" . }}{{.Values.rime.featureFlagServer.name}}
  minReplicas: {{ .Values.rime.featureFlagServer.hpa.minReplicas }}
  maxReplicas: {{ .Values.rime.featureFlagServer.hpa.maxReplicas }}
  metrics:
{{ toYaml .Values.rime.featureFlagServer.hpa.metrics | indent 4 }}
{{- end }}
