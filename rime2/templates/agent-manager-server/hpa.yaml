{{- if .Values.rime.agentManagerServer.hpa.enabled }}
apiVersion: autoscaling/v2beta1
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "rime.fullname" . }}-{{.Values.rime.agentManagerServer.name}}
  labels:
    app: {{.Values.rime.agentManagerServer.name}}
    {{- include "rime.labels" . | nindent 4 }}
    {{- with .Values.rime.agentManagerServer.hpa.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "rime.fullname" . }}{{.Values.rime.agentManagerServer.name}}
  minReplicas: {{ .Values.rime.agentManagerServer.hpa.minReplicas }}
  maxReplicas: {{ .Values.rime.agentManagerServer.hpa.maxReplicas }}
  metrics:
{{ toYaml .Values.rime.agentManagerServer.hpa.metrics | indent 4 }}
{{- end }}
